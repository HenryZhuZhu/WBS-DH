# DE-WBS 项目上下文

更新时间：2026-09-08

## 用户已确认的设计与实现约束

1. **不考虑移动端设计。** 本项目面向桌面端使用，不需要新增移动端布局、移动端导航或移动端适配流程。PRD 的桌面端最小设计尺寸为 1180 × 720。
2. **不使用 Google Fonts（谷歌字体）。** 避免远程字体请求影响原型加载速度；不得引入 Google Fonts 的样式表、字体文件请求或相关加载脚本。优先使用本机系统字体，沿用 `Aptos, "PingFang SC", "Microsoft YaHei", sans-serif` 的字体降级顺序，不因本机缺少某种字体而自动下载远程字体。

以上两项由用户在 2026-09-08 明确确认，适用于后续原型修改与产品实现。

## 项目用途

DE-WBS 是面向 DRAM 设计与 Layout 的 Block 工作分解、进度和 Check 管理工具，目标是将原有飞书/Lark 表格中的层级、Owner、日期、状态和审计信息结构化。

当前目录为需求文档与交互原型，尚未发现完整前后端工程、数据库、构建配置或独立测试工程。

## 文件与版本

| 文件 | 用途与当前状态 |
|---|---|
| `DE-WBS-development-PRD.md` | 前后端开发评审基线，v1.0，文档日期 2026-09-02；包括业务规则、模型、API 建议、导入和验收标准 |
| `DE-WBS-prototype-v1.html` | 单文件 HTML/CSS/JavaScript 交互原型，内置演示数据；当前修改对象应以任务明确指定的文件为准 |
| `DE-WBS-prototype-v1.zip` | 打包的 HTML 原型快照 |
| `DE-WBS-prototype-v1-edge-fixed-3a6d7efe1b36.zip` | 与上一个 ZIP 内的 HTML 内容一致 |
| `README.md` | 当前只有项目标题 |
| `CONTEXT.md` | 本上下文记录，保存用户约束、项目背景与需要注意的差异 |

2026-09-08 核对结果：两个 ZIP 内的 HTML 完全一致，但与目录中的独立 HTML 不同。不得假设压缩包和独立文件属于相同版本，也不得直接用压缩包覆盖当前原型。

## 核心业务上下文

- 三套 hierarchy：Schematic、Layout、SPF。Schematic/Layout 共享冻结身份与结构，业务 Rail 数据独立；SPF 独立。
- 多 Version Block 在 WBS 中只显示当前选中版本的一行，版本切换时更换整行业务数据；Schematic/Layout 的版本选择同步。
- Check status 由 Check 项派生；Progress 由日期派生，不手工填写百分比。
- WBS Progress 与 Delay 监控口径不同：计划有值、实际为空时，WBS 为 On schedule；Delay 监控会依据业务日期判断是否逾期。
- 父节点完成 Check 受直接子节点完成情况约束；子节点重新打开可触发已完成祖先的 Check 回滚；支持同 hierarchy 内跨 Tier 同名 Block 的 Check 同步。
- 主要功能包括 Fullchip 总览、WBS Hierarchy、Delay 监控、Check 明细、Block 详情、行内新增与删除、日期锁、审计和固定 Excel 模板导入。
- PRD 当前汇总基线按全部底层 Version Record 统计，不能将 WBS 的单行展示规则直接当作汇总去重规则。

## 现有差异与后续工作注意事项

- 当前独立 HTML 已包含 IO IP、Analog IP 专用表及关联逻辑，PRD 没有对应的完整专章。修改这些功能前应结合现有代码和用户最新要求理解行为，不能只按旧 PRD 覆盖。
- PRD 引用的 `decisions.json` 和 `data-model.md` 当前未发现；其引用的 `CONTEXT.md` 在本次操作前也不存在。本文件为本次新建，不代表已恢复历史上下文或历史决策。
- 原型中的日期 `2026-08-25` 和 `Lin Q. / Process Leader` 是演示占位，不能直接作为生产业务日期或真实权限映射。
- 实际角色来源、完整权限矩阵、导入回滚权限与时限等仍属于 PRD 开放项，不自行补成已确认需求。
- 本记录是项目上下文摘要，不替代完整 PRD；用户后续明确确认的要求应及时更新到此文件，并注明日期。

## 2026-09-08 新增业务方向：设备采购与研发就绪 WBS

用户先讨论业务并确认一期方向，随后授权制作可交互的单文件 HTML 原型，用于向客户展示。

### 已确认业务规则

- 每行代表一类设备的采购需求，允许分批下单、分批到货；PO issue date 展示多个日期，并对应采购批次。
- Owner 为负责人，Module 为所属领域，User 为使用者，Sponsor 为出资方；这些字段支持手填。
- 保留原始字段：Owner、Module、Area、Sponsor、User、Vendor、Workstation、Tool Category、Tool count、产地、Total Capex KCNY、RMID、有价 PO issue date、Hookup 费用、Total Capex USD$M、YEAR、Category（新 EC 申请）。
- KCNY 与 USD$M 需要联动换算；按此前字段顺序，原型采用 KCNY 两位、USD$M 三位小数。Hookup 三位小数。
- RMID 同一字段允许数字或日期，数字编号按字符串保留，不丢失前导零。
- 设备关联研发阶段或里程碑。同一设备可关联多个里程碑，各阶段按累计需要数量判断，不累加成重复采购需求。
- 使用者签字确认与验收通过必须保持一致，作为同一次完成动作；仅已完成签字验收的数量计入正式可用，允许部分数量完成。

### 一期已接受方向

1. 完整设备 WBS 与采购、到货、签字验收批次。
2. 里程碑设备就绪视图：当前可用与预计按期可用分开，日期未知时标记待确认。
3. 从里程碑缺口追踪至采购批次、Owner、行动与期限。
4. 周会基线与变化摘要，保留行动跟进与历史。
5. 同一数据集的搜索、筛选、分组与保存视图，支持责任、供应商、出资方等角度。

### 原型交付与边界

- 新原型：`Equipment-WBS-prototype.html`，沿用原有 DE-WBS 深色侧栏、蓝色强调、桌面端密集表格、详情抽屉与弹窗视觉语言。
- 原有 `DE-WBS-prototype-v1.html` 与原 PRD 保留，未将设备业务覆盖到设计 Block 业务中。
- 新原型单文件、无外部字体/脚本依赖，浏览器本地保存演示数据；并非后端生产系统。
- 演示业务日期固定 2026-09-08，汇率初值 1 USD = 7.2 CNY，均为演示设定。修改汇率时保留 KCNY，重新换算 USD$M。
- Hookup 暂按 KCNY 独立列示，不自动追加到 Capex；此单位和包含关系尚需业务确认，不作为已确认需求。
- 输入姓名模拟使用者签名，不构成真实电子签署或身份鉴权。
- 导入导出为 Excel 可编辑的 UTF-8 CSV 主字段模板；不支持 XLSX 解析，批次、里程碑与签名记录不在 CSV 范围。
- 使用「演示说明 → 恢复初始演示数据」可重置；清空浏览器存储也会清除本地修改。

### 2026-09-08 原型导航调整

- 左下角用户信息替换为侧边栏展开/收起按钮，收起后保留导航图标与悬停名称；状态在浏览器中持久化。
- 用户明确设备项目不叫 DDCPC：移除顶部项目名称，并清理新原型侧栏及导出文件名中的 DDCPC。未指定新项目名，暂不编造。

### 2026-09-08 页面文案统一

- 用户确认将「周会与变化」改为「进展跟踪」，「查看本周变化」改为「查看最新进展」。
- 对用户展示的「基线」改用「上次记录」「对比起点」；重新比较时明确历史记录和未完成行动保留。
- 「可用数量净变化」按数值方向显示「可用设备增加」「可用设备减少」，无变化时显示「可用设备数量变化」。
- 将「重算」「去重汇总」「独立列示」「管理闭环」等表述改为具体、通俗的操作结果。
- 保留「累计需要数量」的业务含义，并解释包含前期已准备好的设备，不重复采购。
- 仅调整文案，不改变数量、日期、金额、验收和对比计算规则。

### 2026-09-08 产品名称确认

- 当前设备采购原型的正式展示名称为 **WBS-DH**，替换原型左上角的 DE · WBS，并同步网页标题。

### 2026-09-08 精简演示导览

- 已按用户确认删除首页演示导览按钮、导览条和相关状态逻辑。
- 帮助中保留说明与恢复初始演示数据，关闭帮助后保持当前页面。
- 其他界面精简项仅供讨论，未经确认不改动。

### 2026-09-08 全部界面精简项已确认并执行

- 顶部隐藏交互演示标签、固定日期和占位头像，保留帮助入口及帮助内演示说明。
- 删除侧栏项目说明区、页面通用副标题；进展跟踪保留必要的对比日期。
- 首页移除重复的执行数量模块与投入分布图，保留关键指标、里程碑和待处理事项；投入仍可通过 WBS 分组汇总查看。
- 删除里程碑卡片通用提示。
- 常用视图仅默认显示全部、我负责、待验收和无法按时备齐；供应商、出资方与自定义视图放入更多视图。当前「我负责」使用演示负责人李敏，并显示其姓名。
- 表格、抽屉计算与金额规则收纳到可点击、键盘展开的问号说明；实际表单的必要操作提示保留。
- 进展跟踪的最近更新默认折叠，展开查看历史。

### 2026-09-08 一期功能进一步收缩（最新确认）

- 导航只保留项目总览、里程碑、设备 WBS。
- 删除自定义视图保存功能，保留固定视图、搜索、筛选、分组。
- 删除独立进展跟踪、手动对比起点和数量/费用基线比较；总览默认折叠展示最近变更，设备详情保留历史。
- 删除独立行动中心。问题在设备详情维护当前问题、下一步、处理期限，责任人跟随设备 Owner；总览汇总未解决问题。已有问题数据保留，不因精简丢失。
- 不再显示或维护需管理者协调标记。
- 删除打印/PDF专用入口，保留 CSV 导出。
- 里程碑日期在项目建立时确定，现阶段只读；项目设置中可查看。预算汇率移入项目设置，金额自动换算保留。
- 新增和普通补录采购批次不强制填说明；已有批次数量调整、预计日期推迟或清空时必须说明原因。
- 正常签字验收保留签名、数量、日期和确认动作，补充说明选填。
- 原始设备字段完整保留，不增加必填限制。

### 数量文案调整

- 用户确认不再使用「台套」。原型通用数量单位改为「件」，表格字段明确为需求数量、下单数量、到货数量、可用数量；有明确数量列名时不重复单位。

- 用户确认年份字段展示名称统一为 `Year`，替换 `YEAR`；原型表头、详情与导入导出模板同步更新。

### 设备 WBS 列交互

- 执行视图和全部原始字段的每列均支持拖动调宽，标题过长显示省略号，悬停查看完整标题；列宽在当前浏览器保存。
- 所有列支持独立筛选，模糊查找选项并多选，列间按 AND、列内按 OR 组合，并与全局搜索和负责人筛选组合。
- 选项合并用户提供的预设值与当前数据，支持空白筛选；日期、金额、数量也可筛选。筛选输入用于查找选项，勾选并应用后生效。

### 里程碑自定义与 Leader 权限（最新确认）

- 用户确认里程碑可由 Leader 自定义，覆盖此前只读设定。
- 项目设置支持新增、改名、调整日期及删除里程碑；调整日期需说明原因，删除已关联里程碑时明确展示受影响设备并确认。
- 日期调整改变阶段顺序时校验设备累计数量，避免产生后续阶段需求低于前期的矛盾。
- 用户明确不提供角色切换；当前原型固定 Leader 视角，正式系统根据登录用户名对应的身份识别权限，须由服务端执行授权。
- 自定义里程碑在浏览器中保存，设备关联、首页和里程碑视图同步更新。

- 里程碑卡片移除阶段编号说明与「累计设备需求」文案；日期图标替换为「计划完成日期：」。首页和里程碑页的共用卡片同步调整，累计数量计算不变。

- 里程碑卡片在阶段名称前显示动态数字序号（1、2、3……），随当前阶段顺序更新；不修改实际里程碑名称。

- 总览中的研发里程碑改为简洁列表，显示阶段、计划完成日期、状态和风险设备种类数，并单独提示待确认数量。点击进入对应里程碑；里程碑页面保留完整卡片和设备明细。

- 待验收入口同时显示件数和需求条数；待验收视图说明一条需求可包含多件设备，每行标出待验收数量并可直达采购验收详情。WBS 页增加返回项目总览按钮。初始案例为高温试验箱 1 件、探针测试平台 2 件，共 3 件 / 2 条需求。

### 阶段数量改为用户维护（最新规则，覆盖此前验收门禁）

- 里程碑明细的累计需求、可用、预计按时可用三个数量由用户直接填写，可独立调整，非负整数；可用和预计数量可留空表示待填写。
- 系统只计算预计缺口与状态；采购、到货、验收不再覆盖阶段填写值，也不是录入前提。
- 字段修改保留 Leader、时间、阶段、字段名和前后值；不采用覆盖/恢复自动计算模式。
- 原有演示数据一次性转成阶段初始值保存；后续批次变化不影响填写值。新增阶段数量从待填写开始。
- 采购与验收为选填补充记录；总览与 WBS 的验收记录数量明确标示，不能混称为阶段可用数量。
- 允许跨阶段数量独立调整，不再要求阶段需求递增或小于采购总需求；阶段日期变更也不再被数量顺序阻断。

- 总览指标卡精简为名称与主数字：设备需求、已满足需求的里程碑、预计无法按时备齐、设备总费用。前三项整卡点击，费用只展示；移除卡片底部链接与说明。
- 里程碑汇总列表支持点击行或阶段名称进入详情，不再重复显示查看详情链接。里程碑页增加返回总览入口，风险设备视图明确显示筛选条件。

### 一期数量指标简化（最新确认）

- 每个阶段仅需用户维护「需要数量」「已准备数量」，系统计算「还缺数量 = max(需要数量 − 已准备数量, 0)」。
- 状态仅为已备齐、未备齐、待填写；任一必要数量为空则待填写，空白不当作零。未备齐不代表延期。
- 移除阶段预计按时可用数量的展示、编辑和计算依赖，历史修改记录不删除。
- 首页、里程碑卡片/详情、设备准备状态与筛选同步采用上述规则；采购和验收仍为补充记录。

- WBS「准备状态」列名旁增加问号，Hover 或键盘聚焦显示阶段与设备汇总判定规则，包含空值、未关联处理及「未备齐不代表延期」。

### 设备表统一字段视图（最新确认）

- 删除设备 WBS 执行视图和字段视图切换栏，仅显示完整原始字段。
- 保留搜索、列筛选、多选、固定条件筛选、分组及列宽调整；不再在设备主表混入阶段准备状态等派生列。
- 阶段需要/已准备/还缺数量及准备状态仍在里程碑页查看维护。

- 再次确认：设备 WBS 仅一张完整原始字段总表，不保留执行视图或字段显示切换。移除残留视图状态；「常用视图」更名「快捷筛选」，强调只筛选同一张总表的数据。

### 总览指标与设备总表对齐

- 顶部指标统一为设备需求条数、Tool count 合计需求数量、非空 Vendor 去重数量、设备总费用 USD$M。
- 费用使用总表 KCNY 按项目汇率换算，Hookup 不自动追加；供应商去重忽略首尾空格和大小写。
- 总览统计全表，不受之前的筛选影响；点击卡片进入同一总表并清空旧筛选，供应商卡按 Vendor 分组。总表摘要统计当前筛选结果，未筛选时与总览一致。
- 里程碑列表及问题跟进保留为独立项目管理信息，不混入顶部设备总表指标。

### 总览按负责人决策重点重排

- 顶部展示需求总投入、设备需求、已登记 PO 的需求、尚未登记 PO 的需求；均来自同一设备总表。PO 指至少登记一次，不代表数量已全部下单。
- 中间按 Module 和 Sponsor 汇总需求投入，并按 EC Category 原文展示需求条数与总额；不将类别当审批状态，也不推算新 EC 申请金额。
- 保留设备问题跟进，里程碑关联需求默认折叠为次要信息，不代表研发阶段完成情况。
- 点击金额分类、EC 类别和 PO 指标进入同一 WBS 总表筛选结果。投入不代表已付款、已批准或超预算。

### 里程碑以 Leader 达标判断为主（最新确认）

- Leader 填写达标条件、判断（正常推进／有风险／已达标）及说明；未填写默认待判断，不由设备数量自动决定。
- 看板优先显示上述判断、日期、关联需求数、关键缺项、关联投入；总览的里程碑达标情况默认展开。
- 每条设备按阶段独立标记「影响本阶段达标」，标记且未备齐或待填写才计为关键缺项，优先排序。
- 设备准备数量与判断作为依据，不覆盖 Leader 的结论。判断、条件与关键标记修改保留记录。
- 问题跟进继续通过设备详情维护 Owner、下一步和处理期限。
- 关联投入仅供各阶段参考，同一需求可重复关联，不能跨阶段相加作为总投入。

### 里程碑详情聚焦关键需求与问题

- 阶段设备数量表替换为关键需求与问题列表，展示设备、Owner、影响达标标记、当前问题、下一步、处理期限。
- 默认显示标记影响该阶段且有未解决问题的设备，可切换全部关联需求；没有问题不自动等于达标。
- 阶段需要数量、已准备数量和还缺数量移入设备详情对应阶段，可编辑并保留字段修改记录，不新增重复问题填报。

### Leader 项目总览最终调整

- 顶部为需求总投入、近期里程碑、有风险的里程碑、影响达标的未解决问题。
- 近期里程碑取最早尚未由 Leader 标记已达标的阶段，包含已过计划日期但未完成的节点；不自动判断风险。
- 风险数量仅统计 Leader 的有风险判断；未判断仍显示待判断。
- 关键问题按未解决问题记录去重统计，关联多个阶段只计一次，阶段列表分别显示相关问题数量。
- 主体依次为里程碑达标情况和待推动问题；问题优先显示影响达标项，同类按期限排序。
- 投入分布仅一个模块，支持 Module、Sponsor、Year 切换；采购规模、PO 登记和 EC 分类默认折叠。
- 点击风险卡筛选阶段列表，点击关键问题卡筛选问题列表，可恢复全部；点击设备问题沿用原设备详情。

### 详情页与 Leader 总览对齐

- 详情页主指标替换为当前设备的关联里程碑数、Leader 判断有风险的关联节点数、关键未解决问题数和未解决问题数；使用总览同一关联与问题数据，统计范围为当前设备。
- 阶段与问题、问题与记录页面同步显示阶段达标条件、Leader 判断、说明及影响达标标记，支持直接更新。
- 删除详情头部数量派生状态和主区域采购数量流程；阶段数量保留为默认收起的参考信息，采购验收仍在选填页。

- 用户确认隐藏 EQ-001 等示例编号：表格和详情不展示需求内部 ID，保留内部标识用于数据关联。

- 删除总表「快捷筛选」整行，保留搜索、列筛选、负责人/状态筛选和分组；总览跳转携带的筛选仍有效。

### Workstation 详情仅展示原始行数据

- 用户要求详情不增加逻辑：抽屉仅按总表顺序展示该行全部17列，金额与PO日期沿用表内数据。
- 移除详情中的指标、页签、阶段判断、问题和采购验收操作入口，详情为只读展示，字段编辑仍在总表进行。

- Workstation 详情按需求信息、责任归属、采购信息、费用与年度分区，仍只展示原始17列。详情每次从当前行读取，表内保存后统一刷新，不复制独立数据。当前表内15个字段可编辑，Workstation为详情入口、PO日期为现有记录展示。

### 总表下拉字段与单一 PO 日期

- Workstation、Owner、Module、Area、User、Vendor、Tool Category、产地、EC Category 统一使用明确的下拉选择，选项合并用户预设与已有值；保留手动输入。Year 沿用年份下拉。
- Workstation 名称可下拉修改，旁边箭头打开详情。Sponsor 无预设选项，保持直接填写。
- 有价 PO issue date 改为原生 date 控件，只显示一个日期。旧数据默认显示最早的 PO 日期；手动修改/清空后采用总表 poDate 字段，原批次记录不删除。
- 详情、日期列筛选及总览 PO 登记统计统一使用该单一日期，修改保留历史。

- Workstation 列取消下拉选择，直接展示名称并点击查看详情；列筛选和其他字段的下拉选择保持不变。

- 里程碑展示文案统一：Leader 判断 → 阶段状态，判断说明 → 状态说明，编辑按钮 → 更新状态。状态仍由 Leader 维护，选项和计算规则不变。

- 里程碑关键需求与问题表删除「影响达标」列。仅在全部关联需求的行操作中显示设为关键需求/取消关键标记，关键待处理筛选规则不变。

### 问题详情与 Workstation 行详情分开

- 问题名称按问题 ID 打开独立问题抽屉，展示问题、关联需求、影响阶段、Owner、下一步、期限和状态。
- 支持编辑指定问题、标记解决/重新打开，更新后抽屉和总览、里程碑使用同一问题数据。
- Workstation 名称继续打开原始字段详情；问题抽屉内关联需求可跳转行详情。
- 同一需求有多个问题时，各标题打开对应问题；没有未解决问题的「记录问题」打开填写表单。

- 总览模块标题「需要推动的问题」更名为「关键待处理问题」；此次仅调整名称。

- 需求投入分布各分组显示金额及占总需求投入百分比，保留1位小数；领域/出资方/年度同步适用，总金额为0时显示0.0%。

- 总览移除最近变更。总表增加修改记录入口，按需求名称、字段关键词和日期查找；行详情底部折叠展示自身字段历史。阶段状态历史放在里程碑页，问题历史放在对应问题抽屉。新问题事件绑定问题 ID，历史数据按原有标题匹配，保留不删除。

- 用户要求删除里程碑设为关键需求和关联需求操作：删除设为关键/取消关键标记、编辑关联入口及对应修改函数。现有历史关联和标记保留，仅用于原有数据展示，不再提供上述编辑能力。

- 里程碑卡片入口改为查看详情；阶段内容标题为「阶段名称＋阶段详情」。删除关键待处理/全部关联需求 Tab，固定展示当前阶段全部需求与问题。

- 问题详情删除「问题与影响范围」中的关联需求字段；阶段详情表支持 Owner 下拉编辑、问题与下一步文本编辑、处理期限日期控件。问题修改按问题 ID 保存前后值，复用现有数据并同步总览与问题详情。无问题时可通过填写问题创建，Workstation 名称仍作为行详情入口。

## 2026-09-14 分段式项目管理原型

- 最新客户方案为 `WBS-DH-分段式项目管理系统-设计方案-v1.0.md`。按用户确认更新原型，新增 `WBS-DH-prototype.html` 为当前通用项目管理入口；原设备和设计 Block 原型不覆盖，不把设备行自动映射为 Case。
- Project → Case → Task，子项目即 Case。Task 的一级或多级验收由任务性质决定，Task、Case、Project 分别验收，不由组织层数决定 Task 验收层数。固定陈昊 / Leader 视角，不增加角色切换器。
- 新原型独立 localStorage 键 `wbs-dh-project-v1`。所有代执行、代验收弹窗显示对应人员与“模拟操作身份”；核心函数验证执行人与验收人分离。生产环境仍需服务端鉴权。
- 已实现：Project/Case 创建及查看、模板自动生成 Task、Evidence 文件上传与下载、退回重提、多级验收及并行汇合、Case 与 Project 独立提交和验收、历史记录。
- Category 自定义、模板任务周期/Evidence/人员/依赖配置、发布版本与在途 Case 隔离；甘特图按所属 Case Category 分组，不新增 Task 分类。
- 通知按建议的 D-3/D-1/D、D+1/+3/+7 规则生成，切换到验收阶段后提醒当前验收人，支持同日去重、双渠道模拟记录与单渠道重试。业务日期初值 2026-09-14，可向后推进。
- 周/月报表生成、历史与 CSV 导出；提供角色配置与可见范围预览。通知参数和报表计划在此版演示中为工作区共用配置，Project 仍分别按自身日期判断超期。
- 演示约定：只调整未开始的 Task；下层全部通过后，由对象负责人单独提交上层验收；退回后重新进行所需验收；模板页面配置一级或两级作为多级示例，核心支持多级数组。以上不视为客户已经冻结的生产权限、变更或审批细则。
- 本地限制：Evidence 单文件 750 KB、本次总量 2 MB；受浏览器存储总配额限制，保存失败会回滚本次数据修改并提示。无外部邮件/Hems发送，页面关闭时不运行后台任务。页面开启时每分钟检查，自动通知遵守北京时间的配置时点；“检查当前时间线”及日期演示可手动检查。
- 文件同步：根目录与 `DH-WBS/WBS-main/` 的新版 HTML、说明、测试和方案保持一致；更新 WBS-DH.zip 中的相应文件，并核对 HTML 哈希。下载按钮直接下载原始 HTML，不导出浏览器中的业务数据。
- 验证：Node 核心回归测试，浏览器上传/验收/并行汇合/版本隔离/刷新持久化/通知去重与渠道重试/报表，以及 1180、1440、1920px 桌面视口。新版说明见 `WBS-DH-DEMO.md`。

### 2026-09-14 模板配置列名冻结

- “配置新版本”弹窗的任务表格采用独立滚动区域，所有列标题在纵向滚动时固定于表格顶部，任务配置、验收与发布逻辑不变。
- 仅作用于 `#templateDraftTable`；其他表格和弹窗保持原样。验证覆盖 1180×720、1440×1000 桌面视口及长模板草稿，不发布测试数据。

### 2026-09-14 模板开始条件展示

- 用户确认把模板“前置 Task”改为“开始条件”，默认以完整业务描述显示，点击“调整”才展开任务选择，支持收起。无依赖为“Case 启动后”，单项为“任务验收通过后”，多项为“所选任务均验收通过后”。
- 仍使用原有 `deps`，不修改任务启动、并行汇合、验收或版本发布规则。任务改名和选择变化同步更新描述，折叠与表格重绘不清空已选条件，循环依赖校验保留。
- 表头冻结继续有效。原有模板版本和在途 Case 不变，浏览器测试只操作未发布草稿并还原。

### 2026-09-14 模板发布记录入口精简

- 用户确认保留版本追溯、弱化历史入口：卡片明确显示“当前版本 vN”，原“X 个版本”改为“发布记录”，仅多个版本时显示，只有一个版本时隐藏入口，不删除历史数据。
- 弹窗命名为“发布记录”，继续只读展示发布日期、版本任务及创建时采用该版本的 Case 数量。保留“配置新版本”入口，新建 Case 使用最新发布版，已有 Case 保留原版本；不新增切换、回滚或迁移功能。
- 回归覆盖入口条件、只读历史、排序与版本隔离；浏览器验证单/多版本及 1180×720、1440×1000 下的布局，未发布测试版本或修改用户数据。

### 2026-09-14 独立设备 WBS 导航整合

- 用户确认将 `Equipment-WBS-prototype.html` 的设备 WBS 页整合到当前原型，相关业务逻辑以旧原型为准。新增当前导航“设备 WBS”，不移入旧总览、里程碑导航，不自动绑定 Project/Case/Task。
- 设备页独立于当前 Project，顶部不显示项目选择及新版业务日期；预算汇率在设备页内维护，“返回项目总览”回到新版总览。旧准备状态及其里程碑支撑数据保留，EC Category 与 Case Category 不合并。
- 实现复用同目录旧 HTML 的 `#embedded-wbs` 模式：只有在父页面内嵌时隐藏旧导航并直接展示设备表。单独打开旧文件仍是原页面，旧数据与金额、PO、CSV、筛选和详情核心函数不改写。
- 原型现在由主 HTML 与设备 HTML 共同交付，ZIP 保留二者及旧文档；下载入口提供完整包与两份原始 HTML。不是自包含的单 HTML，不能只发送主文件后期待设备页可用。
- 设备继续使用旧 localStorage 键 `equipment-wbs-demo-v1`，通过相同文件路径复用；不写入 `wbs-dh-project-v1`。设备全屏/返回采用限定来源的 UI 消息，不传输业务数据；折叠当前侧栏不会重载设备表。
- 验证：独立与整合页共享保存数据；17列、准备状态筛选、列筛选、分组、列宽、行编辑/详情、预算汇率、PO历史、CSV导入与刷新、全屏/Esc/返回、1180/1440/1920px。导出内容与下载调用已检查，但测试工具未捕获真实下载完成事件。测试数据已按操作前备份还原，用户 Project/Case/Task/模板未变。

### 2026-09-14 设备 Mapping、两级分类与当前阶段

- 用户确认：总设备 Mapping 放顶部，不滚动固定；用竖向柱状图，按 Tool count 汇总需求数量，维度和细分可配置，支持 User 等筛选。
- 新增设备类型、子类型／型号、当前阶段三列，原17列相对顺序保留，共20列。类型预置 Tester/Prober/Handler/Oven/Card/Board/Tool，Tester 子项 Tester Magnum 5 EV；通过分类设置补充父子选项，不覆盖 Workstation 或原 Tool/EC Category。
- 原 User 字段支持父子级选择，不额外新增业务列；PTE 下有 PTE CP/FT/Speed/Array。可只选父级，父级筛选包含直接归属和其已配置下级，子级只匹配自身；图表可切一级汇总/下级明细，均不重复累计。
- 当前阶段由用户手动维护整条设备需求的主要推进环节：预备、EC Pass、PR、PO、设计、制造、交付、验收、完成等。不是每件实物状态，不拆数量，不强制逐阶段流转；选择完成不触发正式验收或上层流程。
- Mapping、表格摘要和筛选使用同一 visibleRows；数量是该阶段需求涉及的数量，不是实际制造/交付/验收完成数量。旧准备状态与当前阶段相互独立，旧数据的新字段保持空白并统计到未填写。
- 分类与图表偏好保存在旧设备存储下的 equipmentCatalog、mappingPreferences，仅用户配置时写入；父类型变更清理子类型并记录历史。User 一级变更清空表单子级选择。分类设置当前提供新增，不擅自删除或重命名历史选项。
- 新增/详情/修改历史/列筛选与三列同步；CSV 19列追加新字段、兼容旧16列，保留旧金额校验及RMID前导零。自定义导入值作为原数据选项保留，不推断未知User的层级关系。
- 浏览器验证三种桌面尺寸、两级新增与选择、User筛选/汇总、阶段修改数据隔离、图表参数持久化、旧新CSV、详情20列、空结果、Mapping非吸顶、全屏保留。Node回归34项通过，测试数据恢复用户原始设备数据，不改Project/Case/Task。
