# WBS-DH 分段式项目管理原型演示

更新时间：2026-09-14

## 入口

- [打开新版原型](WBS-DH-prototype.html)
- [查看最新设计方案](WBS-DH-分段式项目管理系统-设计方案-v1.0.md)
- [原设备 WBS 原型](Equipment-WBS-prototype.html)

使用桌面浏览器直接打开主 HTML，无需启动服务或联网。建议 1440px 及以上，最低桌面宽度为 1180px。主原型与 `Equipment-WBS-prototype.html` 需放在同一目录，完整 ZIP 已包含两者。设备页以嵌入模式复用旧原型，独立打开旧文件的页面与业务规则保持不变；不会将旧设备数据转换为 Case 或 Task。

## 初始案例

项目“研发实验室建设”下有 4 个 Case、17 个 Task，分为设备采购、程序开发和产品研发三个 Category。业务日期为 2026-09-14。

- 测试设备采购：设备交付准备待一级验收，场地准备正在执行，两者均完成后才能开始安装调试。
- 程序开发：程序开发与用例准备并行，之后进入集成验证。
- 可靠性设备采购：包含退回修改和超期 7 天任务，便于说明三级通知升级。
- 测量模块研发：下层 Task 全部通过，但 Case 本身待验收，Project 尚未验收。

## 推荐演示顺序

1. **项目总览**：查看 Case、已验收 Task、三级待验收对象和超期工作。下层任务完成数量不等同于项目验收状态。
2. **Case 列表**：进入“测试设备采购与交付”，查看任务周期、执行人、验收人员、前置依赖和状态。
3. **多级验收**：打开“设备采购与交付准备”，先由李敏模拟通过一级验收，再由周岚模拟通过二级验收。第一级通过不完成 Task，也不释放安装任务。
4. **Evidence 与并行汇合**：打开“场地、电力与接口准备”，上传小型文本或 PDF 文件后提交，由对应人员验收。此时两个前置均通过，“设备安装与调试”变为可执行，但 Case 不会自动验收完成。
5. **分层验收**：打开“测量模块研发”的 Case 验收，通过后返回总览，确认 Case 已通过而 Project 仍在执行。Project 验收需各 Case 先完成，且仍需单独提交与确认。
6. **甘特图**：按设备采购、程序开发等 Category 筛选，查看 Case 内任务在同一时间轴上的并行安排。可移动时间窗口、切换 6/12 周，点击任务条查看详情。
7. **模板版本**：在“类型与模板”配置任务周期、Evidence、执行人与验收人、前置关系，发布 v2。旧 Case 保持 v1；创建同类新 Case 后，使用 v2 的任务配置。
8. **时间线通知**：进入通知中心，检查当前时间线。重复检查不重复生成当天相同接收记录；查看超期任务通知和邮件/Hems结果。可模拟单渠道失败并重试，另一渠道不会被重复发送。
9. **周期报表**：生成当前业务日期截止的报表，查看已验收任务、待验收快照、超期任务和退回情况，并导出 CSV。周期配置支持每周或每月。
10. **项目设置**：查看负责人、Project 验收人、监督接收人及角色权限。调整业务日期到未来，可观察超期升级及到期报表生成；恢复初始案例可回到 09-14。
11. **设备 WBS**：从当前导航直接打开独立设备总表，先看顶部 Mapping，再维护下方明细。原 17 列保留，追加设备类型、子类型／型号、当前阶段后共 20 列。图表、搜索、准备状态与列筛选使用同一批数据；预算汇率仍在设备页内维护。

## 设备 Mapping 与两级分类

- Mapping 放在设备页顶部，不随页面滚动固定。竖向柱状图按 `Tool count` 汇总需求数量，不以设备记录条数冒充件数。
- 可选择设备类型、子类型／型号、当前阶段、User、Module、Area、Vendor、Sponsor 作为分组维度；柱内细分可以关闭，或选择上述维度。图表配置在浏览器保存。
- 顶部类型、子类型、当前阶段、User、下级 User 筛选与表格搜索、负责人、旧准备状态及列筛选共同生效，图表和明细始终同范围。全屏表格暂时隐藏 Mapping，退出后恢复，不清空筛选。
- User 可选父级 PTE，也可下钻选择 PTE CP、PTE FT、PTE Speed、PTE Array。选父级筛选包含直接归属父级和所有已配置子级的记录；选子级只看该子级。图表支持一级汇总或下级明细，每条需求只计算一次。
- 设备类型预置 Tester、Prober、Handler、Oven、Card、Board、Tool。Tester 下预置 Tester Magnum 5 EV；其他子类型由用户通过“分类设置”添加，不凭设备名称推断。
- “分类设置”支持新增设备一级类型、归属某一级的子类型、User 父子选项及当前阶段。已存在的自定义数据保留，当前不增加删除、改名或复杂分类迁移。
- 修改一级设备类型会清空该行原子类型，避免出现跨类型的错误组合；修改一级 User 时清空原子级选择。类型和型号不替换 Workstation、Common/Unique 或 EC Category。
- 当前阶段预置预备、EC Pass、PR、PO、设计、制造、交付、验收、完成，可补充选项。每条需求只手动选择一个整体阶段，不拆分该行各件设备的阶段数量，不强制按列表顺序流转。
- 当前阶段以尚未完成工作的主要推进环节为参考，由负责人判断；“完成”表示该需求整体交付目标达到后的手动标记，不自动写入验收记录，也不自动完成 Task、Case、Project。
- 例如数量 5 的需求标记“制造”，图表表示制造阶段需求合计涉及 5 件，不表示实际有 5 件正在制造。当前阶段与旧准备状态独立，PO 日期和批次变更不会覆盖当前阶段。
- 旧记录的新字段保持未填写，图表将其列为“未填写”且仍计入总量。需由用户按真实情况填写，不自动将旧设备分配到类型、子类型或阶段。

建议演示：给一条需求选择 Tester → Tester Magnum 5 EV，选择“制造”，User 选择 PTE → PTE CP；再在 Mapping 中按设备类型分组、按当前阶段细分，切换 PTE 和 PTE CP 筛选，核对图表与明细数量。

## 设备 WBS 整合边界

- 设备页只整合旧原型的设备 WBS 功能，不带入旧总览、里程碑导航或旧项目设置页。
- 设备总表独立于 Project，不随项目切换分组；进入设备页后顶部显示“独立设备总表”，不展示 Project 选择器或新版业务日期。
- 原始 17 列保持相对顺序，新三列位于 Workstation 后，详情仍只读展示同一行字段。CSV 在原 16 列末尾追加设备类型、子类型／型号、当前阶段，共 19 列；仍支持导入旧 16 列模板，新增字段不额外设为必填。CSV 不包含 PO 日期、批次、旧里程碑或阶段修改历史。
- “预算汇率”使用旧汇率设置：保留 KCNY，重新计算 USD$M；Hookup 单独列示，不自动计入总投入。单一 PO 日期不改写旧批次记录，RMID 前导零保留。
- 设备准备状态继续根据旧里程碑需求与已准备数量计算；相关旧数据保留，但不新增编辑入口。已登记验收数量不等于 Task 的验收进度。
- 设备表的 EC Category 与 Case Category 相互独立，不用于创建 Task 模板。设备数量、金额和旧准备状态不进入 Project/Case/Task 的报表或提醒。
- 整合页与同目录独立设备入口使用同一旧设备存储 `equipment-wbs-demo-v1`；导航切换后重新读取已保存数据，不复制到 `wbs-dh-project-v1`。不同文件路径或浏览器的存储策略可能不同，不能把下载 HTML 当作业务数据迁移。
- 全屏作用于当前集成界面，Esc 或再次点击全屏可退出；收起侧边栏不会重新加载设备表或清空当前筛选。

## 操作边界

- 模板卡片明确显示“当前版本 vN”。只有多个发布版本时才显示次要入口“发布记录”，仅用于查看发布日期、历史任务与创建时采用该版本的 Case 数量；单版本隐藏入口但保留历史数据。不提供版本切换、回滚或在途 Case 迁移，新建 Case 仍使用最新发布版。
- “配置新版本”中的任务表格可独立纵向滚动，全部列名固定在表格顶部，便于对照编辑较长模板。
- 模板中的“开始条件”默认显示业务描述：无等待任务时为“Case 启动后”，选择一项时为“该任务验收通过后”，选择多项时为“所选任务均验收通过后”。点击“调整”展开任务选择，收起后保留选择；任务改名同步更新描述。发布前仍校验循环依赖，未发布草稿不影响在途 Case。
- 固定陈昊 / Leader 视角。执行和验收弹窗中的“模拟操作身份”明确显示代办人员，不代表用户真实登录或授权；正式系统必须做服务端鉴权。
- Task 的执行人与所需验收人必须分离；同一 Task 不能重复配置相同验收人。模板示例采用一级或两级，任务性质决定验收要求。
- 所有所需验收通过后 Task 才完成；当前验收人变化、退回或重提不改变计划完成日期。
- 仅允许修改当前 Case 中尚未开始的 Task，不修改公共模板及其他 Case；循环依赖会被阻断，不自动重排下游日期。
- 为演示闭环，采用“下层全部完成后负责人单独提交上层验收”的保守规则。Case/Project 提交权限及复杂退回路径仍需业务细化，不自动回退下层已通过成果。
- 新增 Category 会带入一个可继续配置的基础任务模板。任务周期按自然日示例计算，计划不代表自动开始，更不代表自动验收完成。
- 甘特图使用计划时间，不展示虚构的完成百分比；截止日期到达不会使任务变为完成。分类始终来源于 Case 的 Category。

## 通知与报表说明

- 邮件和 Hems 都只模拟发送，不调用外部系统，不将 Evidence 外发。
- 临期默认提前 3 天、1 天，到期当日不算超期；超期 1/3/7 天升级。验收等待默认 2 天，可在提醒规则中配置。
- Task 升级接收范围为当前处理人/执行负责人、Case 负责人、Project 负责人。Case/Project 使用自身日期，监督接收人未设置时不虚构人员。
- 提醒和报表配置在此版为演示工作区共用。界面已标识这一演示边界，正式产品可按方案细化为项目级策略。
- 页面开启时每分钟检查，自动时间提醒在北京时间配置小时之后生成；手动检查可立即演示。页面关闭期间不运行后台服务。
- 错过多期报表后，在下一次检查时生成合并时间区间的当前快照，不伪造各历史日期的状态；连续周期边界不重复统计。
- 邮件/Hems重试、已读等操作不改变任务状态。权限配置仅为前端演示，不提供生产安全保障。

## 数据保存与交付

- 新版 localStorage 键：`wbs-dh-project-v1`。与原设备原型独立，浏览器或文件来源不同可能使用不同存储空间。
- Evidence 单文件上限为 750 KB，本次上传总量不超过 2 MB；浏览器总体存储配额有限。文件和历史保存在本地，不上传服务器。
- 新版项目管理保存失败会提示并回滚本次业务修改；设备页沿用旧逻辑，保存受限时提示本次修改仅在当前页面有效。清除浏览器存储会删除本地数据。
- 帮助中的“恢复初始演示数据”只恢复新版原型，不清除旧设备原型数据。
- “下载原型 HTML”打开下载选项，可下载完整 ZIP，或分别下载项目管理与设备 HTML。离线使用需把两个 HTML 保持在同一目录；下载不包含个人上传文件及执行数据。
- 根目录与项目目录同名新版 HTML 使用相同源码，ZIP 中同时保留新版和旧设备原型。

## 验证

在原型所在目录运行：

```sh
node --test WBS-DH-prototype.test.cjs
```

回归测试覆盖任务依赖、Evidence、执行验收分离、多级验收、并行汇合、三级独立验收、模板版本隔离、通知日期边界、去重及周期报表。浏览器验证已覆盖文件上传、验收、Case 创建、模板发布、甘特图筛选、通知渠道重试、报表生成及刷新持久化。

设备集成验证覆盖：原有17列、独立/嵌入入口共享存储、搜索及筛选分组、列宽、行编辑与详情同步、预算汇率、PO 历史、CSV 导入与刷新保留、全屏及返回、三个桌面宽度和项目数据不变。CSV 导出已验证生成内容及下载调用；测试工具未捕获实际下载完成事件，不据此宣称验证了系统下载目录中的文件。

Mapping 更新额外验证：20列表格与详情、需求数量及父子User不重复汇总、空筛选与零数量、两级目录新增与刷新持久化、类型切换清理子类型、阶段不影响旧准备/批次/金额数据、旧16列及新19列CSV导入、新字段导出。所有浏览器测试字段、目录及新增设备在结束后按备份恢复。

尚未验证或实现的生产能力：真实身份鉴权、真实邮件/Hems接入、服务器调度、多人协作、集中存储和真实电子签署。